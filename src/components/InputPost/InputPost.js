import "./InputPost.css";
import useHome from "../../utils/useHome";

function InputPostComponent() {
  //Hook
  const { PostPost, OnChange } = useHome();

  return (
    <div>
      
    </div>
  );
}

export default InputPostComponent;
